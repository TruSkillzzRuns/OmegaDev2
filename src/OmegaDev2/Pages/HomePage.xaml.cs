using Microsoft.UI.Xaml.Controls;

namespace OmegaDev2.Pages;

// Home is a pure splash page — the OmegaDev2 art and nothing else.
// Server URL configuration lives on the Diagnostics page; navigation is
// the pane on the left.
public sealed partial class HomePage : Page
{
    public HomePage()
    {
        InitializeComponent();
    }
}
